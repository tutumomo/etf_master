# Cron Job: ETF 健康巡檢

**Job ID:** caae249735e9
**Run Time:** 2026-04-16 08:01:50
**Schedule:** 0 8 * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

ETF_TW 健康巡檢：檢查 dashboard (port 5055) 是否正常回應，檢查 instance state 檔案是否最新鮮，檢查 venv 是否完好。若有異常請給出簡要原因和建議，以繁體中文回報。

## Response

Now I have all the data I need. Let me compile the health report.

---

# 🏥 ETF_TW 系統健康巡檢報告

**巡檢時間**：2026-04-16 08:00 CST

---

## 1. ✅ Dashboard 存活狀態

| 項目 | 結果 |
|------|------|
| HTTP `/` | **200 OK** |
| Uvicorn 進程 | ✅ PID 77885 運行中 |
| `/health` 端點 | ✅ 回應（但有警告，見下方） |
| `/api/intelligence` | ✅ 正常回傳 |
| Python 版本 | 3.14.3 (Homebrew) |
| FastAPI | 0.135.2 |

**結論：Dashboard 服務正常運行。**

---

## 2. ⚠️ API 端點缺失

以下端點回傳 **404 Not Found**（前端可能依賴）：
- `/api/state`
- `/api/orders_open`
- `/api/health`

**原因**：Dashboard `app.py` 只註冊了 `/health`（非 `/api/health`）、`/api/intelligence`、`/api/overview` 等端點，缺少 `/api/state` 和 `/api/orders_open`。若前端有呼叫這些路徑，會靜默失敗。

**建議**：🟡 次要 — 確認前端是否仍呼叫這些路徑，若已遷移至 `/api/overview` 或其他端點則可忽略。

---

## 3. ⚠️ State 資料不一致（持倉 vs 快照）

`/health` 回報兩項警告：

| 警告 | 說明 |
|------|------|
| 持倉與資產快照不一致 | `positions.json` 有 0050 × 1000 股，但 `portfolio_snapshot.json` 的 `holdings` 為空 `[]` |
| 資產快照延遲 23,380 秒 | portfolio_snapshot 最後更新為 `07:53`（今早），但資料來源標記為 `cold_start_initialization`，表示是冷啟動預設值 |

**詳細數據**：
- `positions.json`：0050 數量 1000，成本 180,000，source=paper_ledger，更新於 01:23
- `portfolio_snapshot.json`：holdings=[], cash=0, total_equity=0，source=cold_start_initialization，更新於 07:53
- `account_snapshot.json`：market_value=180,000, position_count=1，更新於 01:23

**原因**：`portfolio_snapshot.json` 在今早 07:53 被重新初始化（cold_start），導致 holdings 清空。但 `positions.json` 和 `account_snapshot.json` 仍保留昨夜 01:23 的資料，兩者不一致。

**建議**：🟠 緊急 — 執行一次 state 同步刷新，讓 portfolio_snapshot 重新從 positions.json 生成，或透過 broker API 進行 live reconciliation。

---

## 4. ℹ️ State 檔案新鮮度

| 檔案 | 最後更新 | 狀態 |
|------|----------|------|
| portfolio_snapshot.json | 04-16 07:53 | ✅ 今早 |
| positions.json | 04-16 01:23 | ⚠️ 約 7 小時前 |
| orders_open.json | 04-16 01:23 | ⚠️ 約 7 小時前 |
| agent_summary.json | 04-16 01:24 | ⚠️ 約 7 小時前 |
| market_intelligence.json | 04-16 01:24 | ⚠️ 約 7 小時前 |
| auto_trade_state.json | 04-16 01:22 | ⚠️ 約 7 小時前 |

**核心 state 檔案（positions, orders, agent_summary 等）最後更新於昨夜 01:23~01:24**，距離現在約 7 小時。由於目前是非交易時段（08:00 CST，台股 09:00 才開盤），此延遲尚在合理範圍內，但 portfolio_snapshot 的 cold_start 重初始化已造成不一致。

---

## 5. ℹ️ Auto Trade 設定

```json
{
  "enabled": true,
  "live_submit_allowed": false,
  "preview_only_mode": true,
  "market_session_open": false,
  "lock_reason": "非交易時段，已自動停用"
}
```

**狀態正常**：preview_only_mode=true 且 live_submit_allowed=false，符合非交易時段的安全設定。上次決策掃描為 4/15 13:02。

---

## 6. ✅ venv 狀態

| 項目 | 結果 |
|------|------|
| venv Python | 3.14.3 ✅ |
| FastAPI | 0.135.2 ✅ |
| shioaji | 1.3.2 ✅ |
| uvicorn | 0.42.0 ✅ |
| starlette | 1.0.0 ✅ |

**venv 完好，所有核心依賴均已安裝。**

---

## 7. 🟡 Dashboard 日誌問題

- `dashboard.log`（Hermes 路徑）：**0 bytes（空檔案）**，日誌未寫入
- `/tmp/dashboard.log`：4/14 的舊日誌顯示 Jinja2 `UndefinedError: 'data_sources' is undefined`

**原因**：模板 `overview.html` 引用了 `data_sources` 變數但未傳入，可能導致 Dashboard 首頁渲染錯誤。目前 `/` 端點仍回傳 200，可能是 fallback 處理。

**建議**：🟡 次要 — 檢查 `dashboard/app.py` 中 `overview()` 路由是否正確傳遞 `data_sources` 給模板。

---

## 📋 總結與優先建議

| 優先級 | 項目 | 說明 |
|--------|------|------|
| 🟠 緊急 | portfolio_snapshot 不一致 | cold_start 導致 holdings 為空，需重新同步 |
| 🟡 次要 | API 端點缺失 | `/api/state`, `/api/orders_open` 回 404，確認前端是否依賴 |
| 🟡 次要 | Jinja2 模板錯誤 | `data_sources` 未定義，首頁可能渲染異常 |
| 🟢 正常 | Dashboard 服務 | HTTP 200，uvicorn 正常運行 |
| 🟢 正常 | venv | 所有依賴完好 |
| 🟢 正常 | Auto Trade | 預覽模式，非交易時段鎖定正確 |
