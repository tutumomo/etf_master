---
phase: "08"
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [skills/ETF_TW/dashboard/templates/overview.html]
autonomous: false
requirements: [UI-STREAMLINE]
---

<objective>
合併「一鍵同步與全量分析 (Full Sync & Analysis)」按鈕，並移除 Dashboard 中冗餘的手動觸發按鈕，清理 UI 佈局。

目的：降低操作複雜度，減少多餘的點擊，並提升用戶決策效率。
產出：精簡後的 Dashboard UI 頁面。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/ETF_TW/dashboard/templates/overview.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: 合併「一鍵同步與全量分析」按鈕</name>
  <files>skills/ETF_TW/dashboard/templates/overview.html</files>
  <action>
    1. 在 `overview.html` 中實作一個新的 JavaScript 函數 `runFullPipelineSync()`，該函數調用 `/api/decision/full-pipeline`。
    2. 在頁面頂部或顯眼位置（例如 Trading Mode 區塊）新增「一鍵同步與全量分析 (Full Sync & Analysis)」按鈕。
    3. 點擊後顯示全域載入提示 (Global Banner)。
  </action>
  <verify>
    <automated>grep "runFullPipelineSync" skills/ETF_TW/dashboard/templates/overview.html</automated>
  </verify>
  <done>按鈕功能已實作並綁定至正確的 API 端點。</done>
</task>

<task type="auto">
  <name>Task 2: 移除冗餘的手動觸發按鈕</name>
  <files>skills/ETF_TW/dashboard/templates/overview.html</files>
  <action>
    從 `overview.html` 中移除以下按鈕：
    - 「立即同步資料」 (Trading Mode 區塊)
    - 「立即更新 🔄」 (Watchlist 區塊)
    - 「立即規則掃描」 (Decision Control 區塊)
    - 「刷新背景資訊」 (AI Bridge 面板)
    - 「生成 AI 建議」 (AI Bridge 面板)
    - 「全部重跑」 (AI Bridge 面板)
    
    保留：
    - 「套用策略」 (頂部)
    - 「切到 LIVE / PAPER」 (Trading Mode 區塊)
    - 「新增關注標的」 (Watchlist 區塊)
    - 「套用規則引擎設定」 (Decision Control 區塊)
    - 「標記 Reviewed / Superseded」 (AI Bridge 面板)
    - 「記錄 Outcome: Tracked / Reviewed」 (AI Bridge 面板)
  </action>
  <verify>
    <automated>grep -v "refreshDashboardState\|runAutoDecisionScan\|refreshAIDecisionBackground\|generateAIDecision\|rerunAIDecisionPipeline" skills/ETF_TW/dashboard/templates/overview.html</automated>
  </verify>
  <done>UI 介面已精簡，冗餘按鈕已移除。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>UI 佈局精簡與一鍵同步按鈕</what-built>
  <how-to-verify>
    1. 啟動 Dashboard：`bash scripts/start_dashboard.sh`
    2. 訪問 `http://localhost:5000`
    3. 確認是否只剩下一個醒目的「一鍵同步與全量分析」按鈕。
    4. 確認原本細碎的同步、掃描、AI 生成等按鈕是否已消失。
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI | 用戶點擊行為驅動後端管線 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Usability | Dashboard UI | mitigate | 確保「一鍵同步」按鈕在執行中處於 Loading 或 Disabled 狀態，防止重複點擊。 |
</threat_model>

<verification>
手動點擊「一鍵同步」按鈕，確認是否能正確顯示 Banner 並觸發後端管線。
</verification>

<success_criteria>
1. UI 變得更加精簡，功能入口統一。
2. 移除所有不必要的手動觸發按鈕。
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-streamline/08-02-SUMMARY.md`
</output>
