---
phase: ui-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skills/ETF_TW/dashboard/templates/overview.html, skills/ETF_TW/dashboard/app.py]
autonomous: true
requirements: [UI-INTEGRATE]
must_haves:
  truths:
    - "「盤感輔助層」與「關注標的」已合併為單一收合區塊"
    - "關注標的列表顯示技術指標 (RSI, MACD, 月線狀態)"
    - "關注標的列表顯示盤感標籤與相對強弱"
    - "關注標的列表包含「交易」按鈕"
  artifacts:
    - path: "skills/ETF_TW/dashboard/templates/overview.html"
      provides: "Merged Watchlist & Intelligence UI"
  key_links:
    - from: "skills/ETF_TW/dashboard/templates/overview.html"
      to: "/api/intelligence"
      via: "AJAX fetch for RSI/MACD"
---

<objective>
合併「盤感輔助層」與「關注標的」區塊，並在單一表格中顯示完整的技術指標與盤感資訊，同時為所有標的提供交易入口。

目的：減少 Dashboard 冗餘區塊，提升資訊密度，並統一操作介面。
輸出：更新後的 `overview.html` 包含合併後的「關注標的與盤感」區塊。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/ETF_TW/dashboard/templates/overview.html
@skills/ETF_TW/dashboard/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 合併 Dashboard 區塊 UI</name>
  <files>skills/ETF_TW/dashboard/templates/overview.html</files>
  <action>
    1. 刪除原有的 `card_tape_context` (盤感輔助層) 與 `card_watchlist` (關注標的) 區塊。
    2. 新增 `card_watchlist_intelligence` 區塊，標題為「關注標的與市場盤感 / Watchlist & Tape Context」。
    3. 整合兩者的表格欄位，新表格應包含：代號、名稱、趨勢 (30D)、RSI/MACD/月線、盤感標籤、相對強弱、操作。
    4. 在「操作」欄位提供「交易」、「詳情圖表」、「移除」三個按鈕（使用 btn-sm 樣式）。
    5. 保留新增關注標的的輸入框與按鈕。
  </action>
  <verify>
    <automated>grep -q "card_watchlist_intelligence" skills/ETF_TW/dashboard/templates/overview.html</automated>
  </verify>
  <done>UI 結構已更新，區塊成功合併且欄位完整。</done>
</task>

<task type="auto">
  <name>Task 2: 強化技術指標顯示與前端邏輯</name>
  <files>skills/ETF_TW/dashboard/templates/overview.html</files>
  <action>
    1. 更新 `initIntelligence` JavaScript 函數。
    2. 修改 `intelEl.innerHTML` 以包含 MACD 數值 (顯示 `macd` 與 `macd_signal`)。
    3. 確保 `intraday_tape_context` 的資料能正確對應到合併後的表格行。
    4. 修改「移除」按鈕邏輯，確保在合併表格中運作正常。
  </action>
  <verify>
    <automated>grep -q "MACD:" skills/ETF_TW/dashboard/templates/overview.html</automated>
  </verify>
  <done>前端指標載入正常，MACD 已顯示，移除功能運作正常。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | Dashboard UI calls FastAPI endpoints for data and actions |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-UI-01 | Tampering | Watchlist API | mitigate | API already uses symbol normalization and existence check |
| T-UI-02 | Information Disclosure | Intelligence API | accept | Data is local and non-sensitive to general public |
</threat_model>

<verification>
啟動 Dashboard 並檢查「關注標的與市場盤感」區塊是否正確顯示合併後的資料，且所有指標（RSI, MACD, MA）皆有數值。
</verification>

<success_criteria>
1. 成功移除舊區塊並顯示合併後的新區塊。
2. Watchlist 中的標的能正確顯示來自盤感輔助層的標籤（如：偏多震盪、強勢噴發）。
3. 技術指標欄位包含 RSI 與 MACD。
4. 提供快速移除按鈕。
</success_criteria>

<output>
After completion, create `.planning/phases/ui-integration/07-01-SUMMARY.md`
</output>
