---
phase: ui-integration
plan: 02
type: execute
wave: 2
depends_on: [ui-integration-01]
files_modified: [skills/ETF_TW/dashboard/templates/overview.html]
autonomous: true
requirements: [UI-INTEGRATE]
must_haves:
  truths:
    - "持倉區塊與關注區塊的交易票據預設股數為 100"
    - "非持倉標的僅顯示「買進」選項"
    - "持倉標的顯示「買進」與「賣出」選項"
    - "交易票據顯示「預覽模式 (Preview Only)」標記"
  artifacts:
    - path: "skills/ETF_TW/dashboard/templates/overview.html"
      provides: "Optimized Trade Logic & Positioning UI"
---

<objective>
優化持倉區塊的交易介面，並統一所有標的的交易邏輯。

目的：符合用戶「小額測試」的習慣（預設 100 股），並根據持倉狀態動態調整買賣權限。
輸出：更新後的 `overview.html` 包含優化後的持倉區塊與統一的交易票據邏輯。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/ETF_TW/dashboard/templates/overview.html
@.planning/phases/ui-integration/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 優化持倉區塊與預設值</name>
  <files>skills/ETF_TW/dashboard/templates/overview.html</files>
  <action>
    1. 在 `positions` 區塊的交易票據中，將預設數量 (value="1000") 修改為 "100"。
    2. 在交易票據標題旁加入 `Preview Only` 標記（使用 tag 樣式）。
    3. 確保 `trigger_suggestions` (交易閾值建議) 的顯示樣式醒目且易於閱讀。
  </action>
  <verify>
    <automated>grep -q 'value="100"' skills/ETF_TW/dashboard/templates/overview.html</automated>
  </verify>
  <done>預設股數已修改，預覽模式標記已加入。</done>
</task>

<task type="auto">
  <name>Task 2: 實作動態買賣邏輯與 Watchlist 交易入口</name>
  <files>skills/ETF_TW/dashboard/templates/overview.html</files>
  <action>
    1. 為合併後的 `card_watchlist_intelligence` 表格中的每個標的新增隱藏的 `ticket-{{ clean_id }}` 行。
    2. 實作條件邏輯：
       - 若標的在 `position_rows` 中（已持倉）：顯示「買進」與「賣出」按鈕。
       - 若標的不在 `position_rows` 中（未持倉）：僅顯示「買進」按鈕，隱藏或禁用「賣出」選項。
    3. 確保 `toggleTradeTicket` 函數能同時支援持倉區塊與關注區塊的觸發展開。
  </action>
  <verify>
    <automated>grep -q "selectSide" skills/ETF_TW/dashboard/templates/overview.html</automated>
  </verify>
  <done>所有標均有交易按鈕，且買賣權限根據持倉狀態動態切換。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | Order submission via Trade Ticket |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-UI-03 | Elevation of Privilege | Sell side on non-held symbol | mitigate | UI hide/disable + backend pre-flight gate check |
</threat_model>

<verification>
開啟 Dashboard，確認未持倉標的的交易票據僅能點選「買進」；確認持倉標的預設數量為 100 股。
</verification>

<success_criteria>
1. 預設交易數量為 100 股。
2. 未持倉標的的交易票據不顯示或禁用「賣出」按鈕。
3. 關注列表標的點擊「交易」能正確展開票據。
4. 所有交易票據顯示「Preview Only」。
</success_criteria>

<output>
After completion, create `.planning/phases/ui-integration/07-02-SUMMARY.md`
</output>
