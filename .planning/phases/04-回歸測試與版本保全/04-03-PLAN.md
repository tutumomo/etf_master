---
phase: 04-回歸測試與版本保全
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified: []
autonomous: false
requirements: ["GIT-01", "GIT-02", "GIT-03"]
must_haves:
  truths:
    - "全專案 Phase 0 到 Phase 4 擁有 atomic 且具意義的 commit history"
    - "程式碼推送到遠端倉庫 (tutumomo/etf_master)"
  artifacts:
    - path: ".git/refs/heads/main"
      provides: "Local commit logs verified"
  key_links:
    - from: ".git"
      to: "https://github.com/tutumomo/etf_master"
      via: "git push origin main"
      pattern: "git push"
---

<objective>
執行版本保全檢查與最終發布，確認所有歷史 Commit 紀錄完整無缺並推送到遠端 Repository。
Purpose: 確保 GIT-01, GIT-02, GIT-03 均滿足，保留整個計畫開發的成果。
Output: 成功推送到 GitHub 之日誌與包含 commit hash 清單的 SUMMARY 報告。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 版本保全檢查與 Commit Hash 搜集 (GIT-01, GIT-02)</name>
  <files></files>
  <action>
  使用 `git log` 搜集自 Phase 0 以來所有的相關 commit hash 與訊息。
  - 確保本階段（Phase 4）產生的所有測試代碼均已被 commit，且訊息合乎規範。
  - 若有尚未 commit 的變更，請先建立 atomic commit。
  - 整理 commit list 準備輸出在報告中。
  </action>
  <verify>
    <automated>git status --porcelain</automated>
  </verify>
  <done>Working directory is clean, 所有變更皆已提交，且能夠列出 commit 清單</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>已經蒐集完畢所有 Commit Hash 與歷史變更記錄，準備進行遠端推播。</what-built>
  <how-to-verify>
    請確認是否要執行推送（Push）。這會將本地端的改動推播至 `https://github.com/tutumomo/etf_master`。
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: 推播至遠端 GitHub 倉庫 (GIT-03)</name>
  <files></files>
  <action>
  確認遠端指向 `https://github.com/tutumomo/etf_master`（或對應之 origin），並執行 `git push origin main`（依實際主分支名稱而定）。
  記錄 push 成功的日誌並置於最終報告內。
  </action>
  <verify>
    <automated>git log origin/main..HEAD</automated>
  </verify>
  <done>指令回傳空（代表本地與遠端同步），push 成功</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Local -> Remote | Git Push 會寫入遠端 Repository |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | Git | mitigate | 確保 `.env`、`auth.json`、`*.lock` 等機敏檔案不會被誤 commit 且存在於 `.gitignore` 中 |
</threat_model>

<verification>
檢視 `git status` 與 `git remote -v`，確認工作區為空且成功推送到指定的遠端 Repo。
</verification>

<success_criteria>
- 所有 Phase 0 到 Phase 4 修改皆有獨立且有意義之 Commit 紀錄
- 成功執行 Git push 到 `tutumomo/etf_master`
- 產出之 SUMMARY 中包含所有要求的 commit hash 證據
</success_criteria>

<output>
After completion, create `.planning/phases/04-回歸測試與版本保全/04-03-SUMMARY.md`
</output>