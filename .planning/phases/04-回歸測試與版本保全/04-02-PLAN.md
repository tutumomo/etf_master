---
phase: 04-回歸測試與版本保全
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: ["tests/test_ui_flow.py", "tests/test_sizing_policy.py"]
autonomous: true
requirements: ["TEST-04", "TEST-05"]
must_haves:
  truths:
    - "持倉票據無法跳過 confirm 流程直接 submit"
    - "sizing 政策變更後能夠即時反映並改變計算結果"
  artifacts:
    - path: "tests/test_ui_flow.py"
      provides: "Test cases for preview/confirm/submit sequential enforcement"
    - path: "tests/test_sizing_policy.py"
      provides: "Test cases for dynamic update of sizing policies"
  key_links:
    - from: "tests/test_ui_flow.py"
      to: "scripts/etf_core/"
      via: "simulate sequence state"
      pattern: "from scripts.etf_core import"
---

<objective>
建立並驗證針對 UI 票據狀態流轉與 Sizing Policy 設定變更的回歸測試。
Purpose: 防堵使用者/系統繞過 confirm 機制，並確保政策的靈活性可以正常生效。
Output: 建立並通過 `tests/test_ui_flow.py` 與 `tests/test_sizing_policy.py`。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立持倉票據狀態流轉回歸測試 (TEST-04)</name>
  <files>tests/test_ui_flow.py</files>
  <action>
  建立測試以模擬 preview/confirm/submit 流程狀態控制邏輯。
  - 設計當 preview 尚未過時，直接呼叫 submit 邏輯必須被攔截/報錯。
  - 確保 confirm 的 flag 存在方可觸發 final submit。
  </action>
  <verify>
    <automated>python -m pytest tests/test_ui_flow.py</automated>
  </verify>
  <done>跳級測試皆能如預期觸發阻擋，循序測試則順利過關</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立 Sizing 政策變更生效回歸測試 (TEST-05)</name>
  <files>tests/test_sizing_policy.py</files>
  <action>
  建立針對 Sizing Policy 的動態載入與計算回歸測試。
  - 模擬設定「變更前」的閾值，取得建議張數。
  - 模擬設定「變更後」的閾值（如修改現金比例上限），再次計算，驗證新結果即時反映變更。
  </action>
  <verify>
    <automated>python -m pytest tests/test_sizing_policy.py</automated>
  </verify>
  <done>Sizing 政策修改後的新計算結果無縫反映於測試結果中</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Testing -> Core | 驗證 UI flow 不應牽涉實際下單 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Elevation of Privilege | test_ui_flow.py | mitigate | 確保流轉機制的狀態鎖定不能在 test 中透過後門輕易篡改，以真實還原 UI 的呼叫限制 |
</threat_model>

<verification>
執行 `python -m pytest` 確保本計畫產出的兩項測試皆全數 PASS。
</verification>

<success_criteria>
- TEST-04 與 TEST-05 情境順利撰寫完成。
- 所有政策及流程防堵機制符合 ROADMAP 預期。
</success_criteria>

<output>
After completion, create `.planning/phases/04-回歸測試與版本保全/04-02-SUMMARY.md`
</output>