---
phase: 01-truth-governance
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [
  "skills/ETF_TW/scripts/etf_core/utils/truth_level.py",
  "skills/ETF_TW/scripts/poll_order_status.py",
  "skills/ETF_TW/scripts/submit_verification.py",
  "skills/ETF_TW/scripts/etf_tw.py",
  "skills/ETF_TW/scripts/adapters/base.py"
]
autonomous: true
requirements: [TRUTH-01, GIT-01, GIT-02]

must_haves:
  truths:
    - "程式輸出明確標註資訊來源等級（如 [LIVE] 或 [STATE]）"
    - "核心查詢邏輯內部具備 metadata 傳遞真相層級資訊"
  artifacts:
    - path: "skills/ETF_TW/scripts/etf_core/utils/truth_level.py"
      provides: "三層真相分級之常數與輔助函數"
  key_links:
    - from: "scripts/etf_tw.py"
      to: "truth_level.py"
      via: "UI indicator selection"
---

<objective>
在程式邏輯中落實三層真相分級，並在 CLI 輸出中明確標註資料來源。

目的：讓使用者一眼看出目前資訊的可靠程度。
輸出：真相層級工具類別、具備層級標註的訂單與持倉查詢回應。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-truth-governance/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立真相層級工具類別</name>
  <files>skills/ETF_TW/scripts/etf_core/utils/truth_level.py</files>
  <action>
    - 建立 `truth_level.py`，定義以下常數：
      - `LEVEL_1_LIVE_DIRECT`: "LIVE"
      - `LEVEL_2_LIVE_UNCONFIRMED`: "UNCONFIRMED"
      - `LEVEL_3_SECONDARY`: "STATE"
    - 提供一個 `format_with_level(text, level)` 的輔助函數，用於生成帶標籤的字串，例如 `"[LIVE] 0050: 100 股"`。
    - **Commit**: `feat(truth): create truth level utility and constants` (per GIT-01, GIT-02)
  </action>
  <verify>
    <automated>python3 -c "from skills.ETF_TW.scripts.etf_core.utils.truth_level import LEVEL_1_LIVE_DIRECT; print(LEVEL_1_LIVE_DIRECT)"</automated>
  </verify>
  <done>定義了標準的層級常數與格式化工具。</done>
</task>

<task type="auto">
  <name>Task 2: 在查詢邏輯中整合層級標註</name>
  <files>skills/ETF_TW/scripts/poll_order_status.py, skills/ETF_TW/scripts/submit_verification.py, skills/ETF_TW/scripts/etf_tw.py, skills/ETF_TW/scripts/adapters/base.py</files>
  <action>
    - 修改 `adapters/base.py`，讓 adapter 的回應物件（如 Dict）能包含 `_truth_level` 欄位。
    - 修改 `poll_order_status.py`：從 API 直接取得的資訊標註為 `LEVEL_1_LIVE_DIRECT`。
    - 修改 `submit_verification.py`：對於尚未在 `list_trades` 出現但剛剛送出的委託，標註為 `LEVEL_2_LIVE_UNCONFIRMED`。
    - 修改 `etf_tw.py`：在顯示持倉 (portfolio) 與訂單 (orders) 列表時，調用 `truth_level.py` 的格式化函數，在 UI 上顯示來源標記。
    - **Commit**: `feat(truth): apply truth level labeling to core scripts and CLI` (per GIT-01, GIT-02)
  </action>
  <verify>
    <automated>grep "LEVEL_1_LIVE_DIRECT" skills/ETF_TW/scripts/poll_order_status.py</automated>
  </verify>
  <done>核心指令的回應已具備來源標記，CLI 顯示已更新。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adapter → Core | 內部資料流，需正確攜帶 metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure (Ambiguity) | Internal Payloads | mitigate | 透過 `_truth_level` 強制傳遞 metadata |
</threat_model>

<verification>
1. 執行 `python3 scripts/etf_tw.py portfolio` (若環境允許) 觀察是否有 [LIVE] 或 [STATE] 標記。
2. 檢查 `poll_order_status.py` 代碼確認邏輯分支。
</verification>

<success_criteria>
1. 程式輸出具備明確的真相分級標籤。
2. 開發者可透過 `_truth_level` 判讀資料可信度。
</success_criteria>

<output>
After completion, create `.planning/phases/01-truth-governance/01-02-SUMMARY.md`
</output>
