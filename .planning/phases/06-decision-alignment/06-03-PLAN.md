---
wave: 3
depends_on: [06-01-PLAN, 06-02-PLAN]
files_modified:
  - skills/ETF_TW/scripts/run_auto_decision_scan.py
autonomous: true
requirements: [ALIGN-03]
---

# 06-03-PLAN: 共識仲裁策略對齊加權 + Wiki 背景注入

## Objective

升級 `resolve_consensus()` 使其能消費 06-01/06-02 產出的 `strategy_aligned` 信號，讓「雙鏈都對齊策略」時的信心等級提升、「任一鏈不對齊」時降級；同時在 AI Bridge 請求中注入 llm-wiki ETF 知識庫背景，讓 AI 的推理具備標的基礎知識。

## Background（現況審查）

1. `resolve_consensus()`（L72-160）接收 `rule_score` 但**從未接收 `strategy_aligned` 信號**，Tier 1/2/3 僅看 action 方向，不看策略品質
2. `_build_agent_reasoning()` 的 `intelligence` 只有量化指標（RSI/動能/BB），**沒有 wiki 知識背景**
3. Tier 1 高信心的條件只是「雙鏈方向一致」，未考慮「是否同時對齊策略」

## Tasks

### Task 1: resolve_consensus 接收並使用 strategy_aligned 信號
<read_first>
- skills/ETF_TW/scripts/run_auto_decision_scan.py (L72-160，resolve_consensus 全函數)
</read_first>

<action>
修改 `resolve_consensus()` 函數簽章，新增可選參數，並在三個 Tier 中調整信心等級：

```python
def resolve_consensus(
    rule_engine_action: str | None,
    ai_bridge_action: str | None,
    rule_engine_symbol: str | None,
    ai_bridge_symbol: str | None,
    rule_score: int | None = None,
    rule_strategy_aligned: bool | None = None,   # NEW: from 06-01 candidate result
    ai_strategy_aligned: bool | None = None,      # NEW: from 06-02 candidate result
) -> dict:
```

信心等級調整邏輯（加入到每個 Tier 的 return 前）：

```python
def _adjust_confidence(base: str, rule_aligned: bool | None, ai_aligned: bool | None) -> str:
    both_aligned = rule_aligned is True and ai_aligned is True
    either_misaligned = rule_aligned is False or ai_aligned is False
    if both_aligned and base == 'medium':
        return 'high'
    if either_misaligned and base == 'high':
        return 'medium'
    if either_misaligned and base == 'medium':
        return 'low'
    return base
```

在每個 Tier 的 return dict 中：
- 計算 `adjusted_confidence = _adjust_confidence(base_confidence, rule_strategy_aligned, ai_strategy_aligned)`
- 回傳 `confidence_level: adjusted_confidence`
- 新增欄位 `strategy_alignment_signal: {'rule': rule_strategy_aligned, 'ai': ai_strategy_aligned}`
</action>

<acceptance_criteria>
- `resolve_consensus()` 接受 `rule_strategy_aligned` 和 `ai_strategy_aligned` 參數
- 雙鏈對齊策略 → Tier 1 中信心保持/提升為 `high`
- 任一鏈不對齊 → Tier 1 中信心從 `high` 降為 `medium`
- 返回 dict 包含 `strategy_alignment_signal` 欄位
- `grep "strategy_alignment_signal" skills/ETF_TW/scripts/run_auto_decision_scan.py` 有結果
- 現有呼叫 `resolve_consensus(...)` 不帶新參數時不報錯（因為有預設值 None）
</acceptance_criteria>

---

### Task 2: AI Bridge 請求注入 llm-wiki ETF 背景
<read_first>
- skills/ETF_TW/scripts/generate_ai_agent_response.py (L43-50，pre_reasoning 載入邏輯)
- skills/ETF_TW/instances/etf_master/llm-wiki/ (確認 wiki 實體頁路徑格式)
</read_first>

<action>
在 `_build_agent_reasoning()` 中，於 `pre_reasoning` 載入後，嘗試讀取候選標的的 wiki 背景頁：

```python
# === Phase D: Load llm-wiki background for top candidate (if available) ===
WIKI_DIR = ROOT / 'instances' / 'etf_master' / 'llm-wiki' / 'etf'
wiki_context = {}

for sym, _, _ in scored[:3]:
    wiki_path = WIKI_DIR / f'{sym}.md'
    if wiki_path.exists():
        try:
            wiki_text = wiki_path.read_text(encoding='utf-8')
            wiki_context[sym] = wiki_text[:800]
        except Exception:
            pass
```

然後在 `candidate['reason']` 中，若 wiki 存在加入簡短摘要：

```python
wiki_note = ''
if wiki_context.get(symbol):
    first_section = next(
        (ln.strip() for ln in wiki_context[symbol].splitlines() if ln.startswith('## ')),
        ''
    )
    wiki_note = f'；Wiki分類: {first_section[3:]}' if first_section else ''
```
</action>

<acceptance_criteria>
- `generate_ai_agent_response.py` 包含 `WIKI_DIR` 路徑構建
- 若 wiki 頁不存在，graceful fallback（`wiki_context` 為空 dict，不影響流程）
- `grep "WIKI_DIR" skills/ETF_TW/scripts/generate_ai_agent_response.py` 有結果
- `.venv/bin/python -c "import scripts.generate_ai_agent_response as m; print('OK')"` 不報錯
</acceptance_criteria>

## Verification

```bash
cd skills/ETF_TW
grep "strategy_alignment_signal" scripts/run_auto_decision_scan.py
grep "WIKI_DIR" scripts/generate_ai_agent_response.py
.venv/bin/python -m pytest tests/ -q -k "consensus or ai_decision" --tb=short
```

## must_haves
- [ ] `resolve_consensus()` 新參數向後相容（None 預設值）
- [ ] `strategy_alignment_signal` 欄位出現在返回 dict
- [ ] wiki 注入失敗不 crash（try/except 防護）
- [ ] 所有現有測試通過
