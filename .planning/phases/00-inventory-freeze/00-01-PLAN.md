---
phase: "00"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skills/ETF_TW/scripts/sys_path_report.py, .planning/phases/00-inventory-freeze/PATH_REPORT.md]
autonomous: true
requirements: [PATH-01, GIT-01, GIT-02]
must_haves:
  truths:
    - "System output current active paths (HERMES_HOME, config path, active profile, ETF_TW working directory)"
  artifacts:
    - path: "skills/ETF_TW/scripts/sys_path_report.py"
      provides: "Path reporting command"
    - path: ".planning/phases/00-inventory-freeze/PATH_REPORT.md"
      provides: "Full path list record"
---

<objective>
建立並執行系統路徑報告腳本，輸出 HERMES_HOME、config path、active profile、ETF_TW 工作目錄的完整清單。

Purpose: 釐清目前生效路徑，作為後續盤點與凍結的基礎。
Output: 腳本及路徑清單紀錄。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create system path report script</name>
  <files>skills/ETF_TW/scripts/sys_path_report.py</files>
  <action>
    建立 `skills/ETF_TW/scripts/sys_path_report.py`。
    腳本應輸出以下內容：
    - HERMES_HOME (環境變數或預設路徑)
    - ACTIVE_PROFILE (由工作目錄推論或環境變數)
    - ETF_TW_WORKDIR (目前腳本所在的技能目錄)
    - CONFIG_PATH (active profile 下的 config.yaml)
  </action>
  <verify>
    <automated>python3 skills/ETF_TW/scripts/sys_path_report.py</automated>
  </verify>
  <done>腳本可正確執行並輸出完整路徑清單</done>
</task>

<task type="auto">
  <name>Task 2: Generate and save path report</name>
  <files>.planning/phases/00-inventory-freeze/PATH_REPORT.md</files>
  <action>
    執行 `sys_path_report.py` 並將輸出結果格式化為 Markdown 存入 `.planning/phases/00-inventory-freeze/PATH_REPORT.md`。
  </action>
  <verify>
    <automated>cat .planning/phases/00-inventory-freeze/PATH_REPORT.md</automated>
  </verify>
  <done>PATH_REPORT.md 包含完整的路徑清單</done>
</task>

<task type="auto">
  <name>Task 3: Commit the inventory tool and report</name>
  <files>.git</files>
  <action>
    使用 git commit 提交新建立的檔案。
    Commit 訊息範例：`docs(phase0): 建立路徑報告工具並紀錄目前生效路徑 (PATH-01)`
  </action>
  <verify>
    <automated>git log -1 --stat</automated>
  </verify>
  <done>變更已提交至 Git 倉庫</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment → Script | 腳本依賴系統環境變數，若環境變數被惡意竄改可能導致路徑讀取錯誤 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Information Disclosure | sys_path_report.py | mitigate | 僅輸出路徑資訊，不包含敏感憑證 |
</threat_model>

<verification>
執行 `python3 skills/ETF_TW/scripts/sys_path_report.py` 確認輸出與現況一致。
</verification>

<success_criteria>
1. 執行路徑報告指令可輸出 HERMES_HOME、config path、active profile、ETF_TW 工作目錄的完整清單。
</success_criteria>

<output>
After completion, create `.planning/phases/00-inventory-freeze/00-01-SUMMARY.md`
</output>
