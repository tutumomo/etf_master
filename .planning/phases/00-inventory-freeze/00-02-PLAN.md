---
phase: "00"
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/00-inventory-freeze/PATH_INVENTORY.md]
autonomous: true
requirements: [PATH-02, GIT-01, GIT-02]
must_haves:
  truths:
    - "Active/legacy file comparison list exists"
    - "Freeze statement exists in the comparison document"
  artifacts:
    - path: ".planning/phases/00-inventory-freeze/PATH_INVENTORY.md"
      provides: "Active vs Legacy mapping and policy declaration"
---

<objective>
建立 active/legacy 檔案對照清單，並明文聲明後續開發只修改 active 路徑下的檔案。

Purpose: 釐清檔案歸屬，防止混改。
Output: 檔案對照清單。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-inventory-freeze/PATH_REPORT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Active/Legacy file comparison</name>
  <files>.planning/phases/00-inventory-freeze/PATH_INVENTORY.md</files>
  <action>
    透過比較 `~/.hermes/profiles/etf_master/skills/ETF_TW` (active) 與 `~/.openclaw/skills/ETF_TW` (legacy) 的檔案清單，
    建立檔案對照表，記錄主要的 state、config、scripts 檔案對應。
  </action>
  <verify>
    <automated>cat .planning/phases/00-inventory-freeze/PATH_INVENTORY.md</automated>
  </verify>
  <done>對照清單已完整呈現兩者的對應關係</done>
</task>

<task type="auto">
  <name>Task 2: State the Modification Policy</name>
  <files>.planning/phases/00-inventory-freeze/PATH_INVENTORY.md</files>
  <action>
    在 `PATH_INVENTORY.md` 中明文寫出：「本專案後續所有修改僅限於 active 路徑（~/.hermes/...），不得修改 legacy 路徑下的副本，除非是為了執行一次性的遷移工作。」
  </action>
  <verify>
    <automated>grep "only modify active paths" .planning/phases/00-inventory-freeze/PATH_INVENTORY.md</automated>
  </verify>
  <done>文件包含凍結政策聲明</done>
</task>

<task type="auto">
  <name>Task 3: Commit the inventory document</name>
  <files>.git</files>
  <action>
    提交檔案對照清單與政策聲明。
    Commit 訊息：`docs(phase0): 建立檔案對照清單與凍結政策聲明 (PATH-02)`
  </action>
  <verify>
    <automated>git log -1 --stat</automated>
  </verify>
  <done>變更已提交至 Git</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A (Document only)

## STRIDE Threat Register
N/A (Document only)
</threat_model>

<verification>
檢視 `PATH_INVENTORY.md` 的表格內容與政策聲明字句。
</verification>

<success_criteria>
1. active/legacy 檔案對照清單已建立。
2. 明文聲明只修改 active 路徑下的檔案。
</success_criteria>

<output>
After completion, create `.planning/phases/00-inventory-freeze/00-02-SUMMARY.md`
</output>
