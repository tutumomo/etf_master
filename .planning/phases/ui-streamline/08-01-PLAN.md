---
phase: 08-ui-streamline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skills/ETF_TW/dashboard/app.py]
autonomous: true
requirements: [UI-STREAMLINE]
---

<objective>
建立 `/api/decision/full-pipeline` 整合端點，實現報價同步、盤感生成、規則掃描與仲裁共識的連鎖執行。

目的：提供單一入口，簡化多個獨立腳本的調用流程，並確保資料新鮮度與決策一致性。
產出：後端 API 端點與對應的腳本調用邏輯。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/ETF_TW/dashboard/app.py
@skills/ETF_TW/scripts/refresh_monitoring_state.py
@skills/ETF_TW/scripts/run_auto_decision_scan.py
@scripts/generate_decision_consensus.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 實作 _run_full_pipeline_helper</name>
  <files>skills/ETF_TW/dashboard/app.py</files>
  <action>
    在 `app.py` 中實作一個 helper 函數 `_run_full_pipeline_helper()`，該函數應依序執行以下腳本：
    1. `refresh_monitoring_state.py` (整合報價同步與盤感生成)
    2. `run_auto_decision_scan.py` (執行規則掃描與 sizing 計算)
    3. `generate_decision_consensus.py` (執行 AI 橋接仲裁與共識生成)
    
    技術細節：
    - 使用 `subprocess.Popen` 或 `subprocess.run` 執行腳本。
    - 確保 `generate_decision_consensus.py` 的路徑正確指向項目根目錄的 `scripts/`。
    - 執行前應先刪除舊的 `decision_consensus.json` 以觸發 UI 的載入狀態。
    - 支援「非阻塞 (Background)」執行模式，並能回傳初始啟動狀態。
  </action>
  <verify>
    <automated>python3 -c "from skills.ETF_TW.dashboard.app import _run_full_pipeline_helper; print(_run_full_pipeline_helper())"</automated>
  </verify>
  <done>Helper 函數實作完成，且能依序啟動所有關鍵腳本。</done>
</task>

<task type="auto">
  <name>Task 2: 建立 /api/decision/full-pipeline 端點</name>
  <files>skills/ETF_TW/dashboard/app.py</files>
  <action>
    新增 POST 端點 `/api/decision/full-pipeline`，調用上述 helper 函數。
    回傳格式：
    ```json
    {
      "ok": true,
      "message": "已啟動全流程同步與分析 (Full Sync & Analysis)",
      "background": true,
      "timestamp": "2026-04-16T10:00:00Z"
    }
    ```
  </action>
  <verify>
    <automated>curl -X POST http://localhost:5055/api/decision/full-pipeline</automated>
  </verify>
  <done>API 端點建立完成，且能正確響應 POST 請求。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard API | 外部請求觸發後端重度運算腳本 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | DoS | /api/decision/full-pipeline | mitigate | 端點僅限本地訪問 (localhost)，且內部應有執行鎖防止併發重疊執行。 |
</threat_model>

<verification>
執行 `/api/decision/full-pipeline` 後，觀察日誌確認腳本依序執行，且 `decision_consensus.json` 最終被更新。
</verification>

<success_criteria>
1. 成功建立 `/api/decision/full-pipeline` 並能啟動整合管線。
2. 報價同步、規則掃描、仲裁共識依序執行無誤。
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-streamline/08-01-SUMMARY.md`
</output>
