---
phase: 08-ui-streamline
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified: [skills/ETF_TW/dashboard/app.py]
autonomous: true
requirements: [UI-STREAMLINE]
---

<objective>
確保「投資策略變動」會自動調用新的 Full Pipeline 整合端點，實現策略更新與全量分析的深度連動。

目的：當用戶調整核心策略時，系統應即時重新同步市場與決策，確保 Dashboard 顯示的是基於新策略的分析結果。
產出：連動後的 `strategy_update` 邏輯。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/ETF_TW/dashboard/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 strategy_update 端點邏輯</name>
  <files>skills/ETF_TW/dashboard/app.py</files>
  <action>
    修改 `app.py` 中的 `strategy_update` 函數：
    1. 將原本調用 `_run_consensus_rescan()` 的部分改為調用 `_run_full_pipeline_helper()`。
    2. 確保策略更新後，系統會執行完整的「同步 -> 盤感 -> 規則 -> 共識」流程。
  </action>
  <verify>
    <automated>grep "_run_full_pipeline_helper" skills/ETF_TW/dashboard/app.py</automated>
  </verify>
  <done>策略更新與 Full Pipeline 成功連動。</done>
</task>

<task type="auto">
  <name>Task 2: 全流程連動測試與驗證</name>
  <files>skills/ETF_TW/dashboard/app.py</files>
  <action>
    執行 UAT：
    1. 切換 Dashboard 策略。
    2. 檢查 API 回傳結果是否包含 `full_pipeline: true`。
    3. 觀察背景腳本執行日誌，確認 `refresh_monitoring_state.py` 與決策掃描被啟動。
  </action>
  <verify>
    <automated>curl -X POST -H "Content-Type: application/json" -d '{"base_strategy":"核心累積", "scenario_overlay":"無"}' http://localhost:5055/api/strategy/update</automated>
  </verify>
  <done>連動測試通過，策略切換可觸發完整同步鏈。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Logic | 內部狀態變更觸發重度非同步操作 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Availability | Strategy Update Linkage | mitigate | 確保連動操作在背景執行，不阻塞 UI 響應。 |
</threat_model>

<verification>
切換策略後，Dashboard 應顯示「同步中」或類似提示，並在數秒內更新為基於新策略的決策共識。
</verification>

<success_criteria>
1. 投資策略變動後能正確觸發 Full Pipeline。
2. 全流程連動符合預期，無死循環或異常報錯。
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-streamline/08-03-SUMMARY.md`
</output>
