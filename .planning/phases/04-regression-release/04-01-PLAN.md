---
phase: "04"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["tests/test_trade_validation.py", "tests/test_trade_verification.py"]
autonomous: true
requirements: ["TEST-01", "TEST-02", "TEST-03"]
must_haves:
  truths:
    - "單位與 odd-lot 回歸案例涵蓋 Common 張數與 IntradayOdd 股數驗證"
    - "list_trades 空回應不反推交易結果"
    - "委託 submit 成功未落地時不報為已成交"
  artifacts:
    - path: "tests/test_trade_validation.py"
      provides: "Test cases for trading unit logic"
    - path: "tests/test_trade_verification.py"
      provides: "Test cases for trade response truth and execution verification"
  key_links:
    - from: "tests/test_trade_validation.py"
      to: "scripts/etf_core/"
      via: "import core trading module"
      pattern: "from scripts.etf_core import"
---

<objective>
建立並驗證針對核心交易單位邏輯與真實性原則的回歸測試。
Purpose: 確保系統在處理單位轉換、查詢空回應以及單純 submit 未落地的語義能夠嚴格遵守交易保險絲收斂與真相層級治理規範。
Output: 建立並通過 `tests/test_trade_validation.py` 與 `tests/test_trade_verification.py` 回歸測試。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立單位與 Odd-lot 回歸測試案例 (TEST-01)</name>
  <files>tests/test_trade_validation.py</files>
  <action>
  建立方針與測試案例以驗證「Common 對應張數、IntradayOdd 對應股數」的輸入，以及預期之錯誤處理。
  - 需要安裝 `pytest` 若系統尚未安裝（可在本任務順便 `pip install pytest`）。
  - 在 `tests/test_trade_validation.py` 中寫入對應測試函式。
  - 確保當給予不合規單位組合（如 IntradayOdd 張數）時拋出正確例外。
  </action>
  <verify>
    <automated>python -m pytest tests/test_trade_validation.py</automated>
  </verify>
  <done>測試案例全數 PASS 且完整覆蓋 Common 張 / IntradayOdd 股的邏輯</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立空回應與未落地語義回歸測試案例 (TEST-02, TEST-03)</name>
  <files>tests/test_trade_verification.py</files>
  <action>
  建立測試案例以驗證 list_trades 空回應語義及 submit 成功未落地情況。
  - 針對 `list_trades`，模擬回傳空清單時，系統回應或日誌不應推斷為失敗或成交，僅能反映「未尋獲資料」。
  - 針對 `submit`，模擬送出委託成功但落地查詢（positions）未發現該筆委託時，不應誤報為交易成功。
  </action>
  <verify>
    <automated>python -m pytest tests/test_trade_verification.py</automated>
  </verify>
  <done>測試案例涵蓋 TEST-02、TEST-03，並全數 PASS</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Testing -> Core | 測試模組呼叫核心業務邏輯時，須確保無修改生產數據 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | test_trade_validation.py | mitigate | 確保所有測試案例使用 mock 依賴或 paper mode，不觸發實際交易 |
</threat_model>

<verification>
執行 `python -m pytest`，確保本計畫產生的測試案例全部順利執行。
</verification>

<success_criteria>
- 測試檔案順利建立並運行無誤
- pytest 能抓取並驗證 TEST-01 到 TEST-03 之情境
</success_criteria>

<output>
After completion, create `.planning/phases/04-regression-release/04-01-SUMMARY.md`
</output>