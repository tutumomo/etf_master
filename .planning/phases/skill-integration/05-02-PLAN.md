---
phase: skill-integration
plan: 05-02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [
  "SOUL.md",
  "skills/stock-analysis-tw/scripts/analyze_stock.py"
]
autonomous: true
requirements: [SKILL-03, SKILL-04]

must_haves:
  truths:
    - "SOUL.md 包含意圖觸發映射表。"
    - "技能分析結果能寫入或參考 instances/etf_master/state/ 目錄。"
  artifacts:
    - path: "SOUL.md"
      provides: "Agent 意圖觸發邏輯"
  key_links:
    - from: "SOUL.md"
      to: "stock-analysis-tw"
      via: "意圖映射"
---

<objective>
建立 `ETF_master` 的意圖觸發映射表 (Intent-Skill Mapping)，並整合技能分析結果至 `instances/etf_master/state/`。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SOUL.md
@skills/stock-analysis-tw/scripts/analyze_stock.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 整合技能結果至 etf_master 狀態</name>
  <files>skills/stock-analysis-tw/scripts/analyze_stock.py</files>
  <action>
    1. 修改 `analyze_stock.py`，增加一個可選參數 `--state-dir`。
    2. 當提供 `--state-dir` 時，將分析結果（特別是評分與關鍵訊號）寫入該目錄下的 `stock_intelligence.json`。
    3. 確保 `ETF_master` 能讀取此檔案以增強其對標的的理解。
  </action>
  <verify>
    <automated>uv run skills/stock-analysis-tw/scripts/analyze_stock.py 0050.TW --fast --state-dir skills/ETF_TW/instances/etf_master/state/</automated>
  </verify>
  <done>分析結果正確寫入 `skills/ETF_TW/instances/etf_master/state/stock_intelligence.json`。</done>
</task>

<task type="auto">
  <name>Task 2: 升級 SOUL.md 建立意圖映射表</name>
  <files>SOUL.md</files>
  <action>
    1. 在 `SOUL.md` 中新增 `## 意圖觸發映射表 (Intent-Skill Mapping)` 章節。
    2. 定義以下映射：
       - **買賣建議/下單預演** -> `ETF_TW` (核心執行與風控)
       - **深度診斷/標的健康度** -> `stock-analysis-tw` (8 維度量化評分)
       - **技術線圖/即時報價/指標** -> `stock-market-pro-tw` (Pro PNG & Indicators)
       - **估值建模/投行視角/戰略分析** -> `taiwan-finance` (References & Valuation)
       - **熱點/異動偵測** -> `stock-analysis-tw (hot/rumors)`
    3. 明確標註當使用者情緒不穩定時，應優先回報 `ETF_TW` 的執行狀態，而非發起深度分析。
  </action>
  <verify>
    <automated>grep "意圖觸發映射表" SOUL.md</automated>
  </verify>
  <done>SOUL.md 已更新且包含完整的映射邏輯。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Script → State Directory | 寫入權限需嚴格限制在 instance 目錄內 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | stock_intelligence.json | accept | 該目錄已在 .gitignore 中，且僅供 Agent 內部使用 |
</threat_model>

<success_criteria>
- 分析結果能持久化到 `etf_master` 的狀態目錄。
- SOUL.md 映射表清晰明確，導引 Agent 調用正確技能。
</success_criteria>

<output>
After completion, create `.planning/phases/skill-integration/05-02-SUMMARY.md`
</output>
