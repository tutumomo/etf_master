---
phase: 00-盤點與凍結
plan: 03
type: execute
wave: 2
depends_on: [00-01]
files_modified: [skills/ETF_TW/scripts/audit_paths.sh]
autonomous: true
requirements: [PATH-03, GIT-01, GIT-02]
must_haves:
  truths:
    - "Grep audit confirms zero results for hardcoded legacy paths in active code"
  artifacts:
    - path: "skills/ETF_TW/scripts/audit_paths.sh"
      provides: "Path mixing audit utility"
---

<objective>
執行程式碼盤點，確認 active 程式碼中無任何地方同時引用或誤用 legacy 路徑（~/.openclaw）。

Purpose: 杜絕路徑混用帶來的資料不一致風險。
Output: 稽核腳本與稽核通過紀錄。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-盤點與凍結/PATH_REPORT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Perform Grep Audit for Legacy Paths</name>
  <files>skills/ETF_TW/scripts/audit_paths.sh</files>
  <action>
    撰寫 `skills/ETF_TW/scripts/audit_paths.sh` 腳本，
    在 `skills/ETF_TW` 目錄下搜索所有包含 `.openclaw` 字串的檔案。
    排除 `.git`、`.venv`、`sessions/` 等非程式碼路徑。
  </action>
  <verify>
    <automated>bash skills/ETF_TW/scripts/audit_paths.sh</automated>
  </verify>
  <done>腳本可執行並列出所有潛在的混用點</done>
</task>

<task type="auto">
  <name>Task 2: Fix any mixed path usage found</name>
  <files>skills/ETF_TW/</files>
  <action>
    若稽核結果發現硬編碼的 `~/.openclaw`，將其修正為：
    - 使用相對路徑。
    - 使用環境變數推論。
    - 使用 active 路徑（~/.hermes/...）。
    特別注意 `state_legacy_compat_link` 這個符號連結，確保程式碼呼叫它是通過 active 根目錄。
  </action>
  <verify>
    <automated>bash skills/ETF_TW/scripts/audit_paths.sh</automated>
  </verify>
  <done>程式碼稽核結果為零混用 (Zero results for illegal paths)</done>
</task>

<task type="auto">
  <name>Task 3: Final verification and commit</name>
  <files>.git</files>
  <action>
    提交稽核工具與修正。
    Commit 訊息：`fix(phase0): 修正程式碼路徑混用問題並建立稽核工具 (PATH-03)`
  </action>
  <verify>
    <automated>git log -1 --stat</automated>
  </verify>
  <done>變更已提交至 Git</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A (Read-only audit & internal fixes)

## STRIDE Threat Register
N/A
</threat_model>

<verification>
執行 `bash skills/ETF_TW/scripts/audit_paths.sh` 並確認無任何關鍵程式碼檔案命中。
</verification>

<success_criteria>
1. 程式碼 grep 確認無任何地方同時引用 ~/.openclaw 與 ~/.hermes/profiles/etf_master 路徑。
</success_criteria>

<output>
After completion, create `.planning/phases/00-盤點與凍結/00-03-SUMMARY.md`
</output>
