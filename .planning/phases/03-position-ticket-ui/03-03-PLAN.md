---
phase: "03"
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [skills/ETF_TW/dashboard/app.py, skills/ETF_TW/dashboard/templates/overview.html]
autonomous: true
requirements: [TICKET-03, TICKET-04, TICKET-05, GIT-01, GIT-02]
must_haves:
  truths:
    - "使用者必須先點擊 Preview，後續才會出現 Confirm 字串"
    - "使用者點擊 Confirm 後，才會出現 Submit 按鈕"
    - "送單請求必須經過 pre-flight 檢查，若檢查失敗則不允許送出"
  artifacts:
    - path: "skills/ETF_TW/dashboard/app.py"
      provides: "/api/trade/preview and /api/trade/submit endpoints"
    - path: "skills/ETF_TW/dashboard/templates/overview.html"
      provides: "Ticketing state management logic (Preview -> Confirm -> Submit)"
  key_links:
    - from: "Frontend Ticketing UI"
      to: "/api/trade/submit"
      via: "Confirm guard"
      pattern: "submit.*confirm"
---

<objective>
實作三段式交易票據流程（Preview -> Confirm -> Submit）。確保交易安全（TICKET-04）並與後端風控系統（Sizing + Pre-flight）深度整合（TICKET-05），提供可靠的交易執行環境。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/ETF_TW/dashboard/app.py
@skills/ETF_TW/dashboard/templates/overview.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: 實作交易預覽 API (/api/trade/preview)</name>
  <files>skills/ETF_TW/dashboard/app.py</files>
  <action>
    - 在 `app.py` 中新增 POST 端點 `/api/trade/preview`。
    - 邏輯：呼叫後端 `sizing_engine` 或 `pre_flight_gate` 進行初步計算與檢查。
    - 回傳：估計交易金額、手續費估計（如有）、風控提示（例如集中度警示）、檢查是否通過。
  </action>
  <verify>
    <automated>grep "/api/trade/preview" skills/ETF_TW/dashboard/app.py</automated>
  </verify>
  <done>後端具備交易預覽能力。</done>
</task>

<task type="auto">
  <name>Task 2: 票據 UI 狀態流轉實作</name>
  <files>skills/ETF_TW/dashboard/templates/overview.html</files>
  <action>
    - 在 `overview.html` 中實作票據的狀態控制 JS。
    - 狀態 1 (Preview): 顯示輸入框與 Preview 按鈕。
    - 狀態 2 (Confirm): Preview 回傳成功後，隱藏 Preview 按鈕，顯示預覽結果摘要與「我已確認...」的確認勾選框或確認字串。
    - 狀態 3 (Submit): 勾選確認後，顯示 Submit 按鈕。
    - 任何輸入變更應將狀態重設回 Preview 階段。
  </action>
  <verify>
    <automated>grep "submit.*style.*display" skills/ETF_TW/dashboard/templates/overview.html</automated>
  </verify>
  <done>前端具備強制的 Preview -> Confirm -> Submit 流程限制。</done>
</task>

<task type="auto">
  <name>Task 3: 實作正式送單 API (/api/trade/submit)</name>
  <files>skills/ETF_TW/dashboard/app.py</files>
  <action>
    - 在 `app.py` 中新增/更新 POST 端點 `/api/trade/submit`。
    - 必須強制檢查請求是否來自已確認的預覽，或在後端重新執行所有 `pre_flight_gate` 檢查。
    - 呼叫真實的下單邏輯（Paper 或 Live）。
    - 回傳執行結果與委託序號。
  </action>
  <verify>
    <automated>grep "/api/trade/submit" skills/ETF_TW/dashboard/app.py</automated>
  </verify>
  <done>交易執行路徑已完整串接，受風控系統保護。</done>
</task>

<task type="auto">
  <name>Task 4: 階段性 Commit</name>
  <files>skills/ETF_TW/dashboard/app.py, skills/ETF_TW/dashboard/templates/overview.html</files>
  <action>
    - 驗證三段式流程，確保留有 Commit 紀錄（GIT-01, GIT-02）。
  </action>
  <verify>
    <automated>git log -1 --pretty=%B | grep "TICKET-03\|TICKET-04"</automated>
  </verify>
  <done>完成 Phase 3 所有核心需求的變更提交。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser → API | 所有的交易參數必須在後端重新驗證，不信任前端 Preview 結果 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Elevation of Privilege | Trade Submission | mitigate | 在 `/api/trade/submit` 強制執行 `pre_flight_gate`，不因 UI 狀態而跳過檢查 |
| T-03-03-02 | Denial of Service | Trade APIs | mitigate | 實作限流或驗證 Token，防止惡意連點送單 |
</threat_model>

<verification>
1. 在票據輸入數量，點擊 Preview，確認出現正確的估計值。
2. 確認 Submit 按鈕在 Preview 之前是隱藏的。
3. 勾選確認後，點擊 Submit，檢查後端 log 是否執行了 `pre_flight_gate`。
4. 故意輸入超額數量，確認 Preview 會顯示錯誤訊息且無法進入 Confirm 階段。
</verification>

<success_criteria>
- 完整實現 Preview -> Confirm -> Submit 流程。
- 禁止跳過 Confirm 直接送單。
- 送單後正確呼叫後端下單邏輯。
</success_criteria>

<output>
After completion, create `.planning/phases/03-position-ticket-ui/03-03-SUMMARY.md`
</output>
