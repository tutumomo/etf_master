---
phase: 03-持倉交易票據UI
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [skills/ETF_TW/dashboard/templates/overview.html]
autonomous: true
requirements: [TICKET-01, TICKET-02, GIT-01, GIT-02]
must_haves:
  truths:
    - "持倉主列（Positions Table）中出現「交易」按鈕，且無其他表單欄位"
    - "點擊「交易」按鈕後，會在該列下方展開一個區塊（Drawer/Inline Detail）"
    - "展開的區塊包含標的名稱與基礎交易表單（買賣方向、數量、委託價格）"
  artifacts:
    - path: "skills/ETF_TW/dashboard/templates/overview.html"
      provides: "Trade button and ticketing detail row implementation"
  key_links:
    - from: "Trade Button"
      to: "Ticketing Row"
      via: "toggleTradeTicket JS function"
      pattern: "toggleTradeTicket"
---

<objective>
實作持倉交易的入口與展開層。確保持倉主列保持整潔（TICKET-01），並將交易功能隱藏在點擊後的展開層中（TICKET-02），提供專業且不擁擠的 UI 體驗。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/ETF_TW/dashboard/templates/overview.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: 持倉主列交易入口實作</name>
  <files>skills/ETF_TW/dashboard/templates/overview.html</files>
  <action>
    - 在「持倉快照」表格中增加「操作」欄位。
    - 在該欄位加入「交易」按鈕（樣式需與「詳情圖表」一致或更顯眼）。
    - 確保表格主列不包含 input 欄位，僅顯示純資訊。
  </action>
  <verify>
    <automated>grep "交易" skills/ETF_TW/dashboard/templates/overview.html</automated>
  </verify>
  <done>持倉表格具備交易入口按鈕。</done>
</task>

<task type="auto">
  <name>Task 2: 實作交易票據展開層 (Detail Row)</name>
  <files>skills/ETF_TW/dashboard/templates/overview.html</files>
  <action>
    - 仿照「詳情圖表」的模式，在每一行持倉下方建立一個 `id="ticket-{{ symbol }}"` 的隱藏列（tr）。
    - 撰寫 `toggleTradeTicket(symbol)` JS 函數，確保同一時間只有一個票據展開，或支援多開（視 UI 寬度而定，建議多開）。
    - 在展開層內設計票據容器，包含標的資訊區與表單區。
  </action>
  <verify>
    <automated>grep "toggleTradeTicket" skills/ETF_TW/dashboard/templates/overview.html</automated>
  </verify>
  <done>點擊交易按鈕能展開/收合專屬的交易票據區塊。</done>
</task>

<task type="auto">
  <name>Task 3: 建立基礎交易表單</name>
  <files>skills/ETF_TW/dashboard/templates/overview.html</files>
  <action>
    - 在票據區塊內加入表單：
      - 買/賣選擇（Radio 或 Segmented Control）。
      - 委託數量（Number Input）。
      - 委託價格（Number Input）。
      - 預覽按鈕 (Preview)。
    - 加入「新手小提示」：例如說明「張」與「股」的轉換（TICKET-07 延伸）。
  </action>
  <verify>
    <automated>grep "qty\|price\|preview" skills/ETF_TW/dashboard/templates/overview.html</automated>
  </verify>
  <done>票據內具備完整的基礎交易參數輸入欄位。</done>
</task>

<task type="auto">
  <name>Task 4: 階段性 Commit</name>
  <files>skills/ETF_TW/dashboard/templates/overview.html</files>
  <action>
    - 使用 `git commit` 提交變更，訊息須包含範圍與目的（GIT-01, GIT-02）。
  </action>
  <verify>
    <automated>git log -1 --pretty=%B | grep "TICKET-01\|TICKET-02"</automated>
  </verify>
  <done>完成本計畫的變更提交。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser → Form | 使用者輸入的參數在此階段僅為前端狀態 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | Ticketing Form | mitigate | 確保展開層不會意外洩漏非必要的帳戶敏感資訊 |
</threat_model>

<verification>
1. 開啟 Dashboard -> 持倉快照。
2. 確認每一行都有「交易」按鈕，且無直接顯示的輸入框。
3. 點擊按鈕，確認下方展開票據區。
4. 在票據內輸入測試數值，確認 input 正常運作。
</verification>

<success_criteria>
- 持倉表格主列整潔，只有「交易」按鈕。
- 點擊後成功展開票據。
- 票據內包含方向、數量、價格輸入與預覽按鈕。
</success_criteria>

<output>
After completion, create `.planning/phases/03-持倉交易票據UI/03-02-SUMMARY.md`
</output>
