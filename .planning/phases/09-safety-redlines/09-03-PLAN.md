---
phase: 09-safety-redlines
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified: [skills/ETF_TW/tests/test_safety_redlines.py]
autonomous: true
requirements: [FUSE-V3-REDLINE]

must_haves:
  truths:
    - "Test case fails if max_buy_shares is exceeded"
    - "Test case fails if max_buy_amount_twd is exceeded"
    - "Test case fails if max_buy_amount_pct is exceeded"
    - "Test case fails if daily_loss circuit breaker is on"
    - "Test case fails if AI confidence is low"
  artifacts:
    - path: "skills/ETF_TW/tests/test_safety_redlines.py"
      provides: "Regression tests for redline system"
---

<objective>
撰寫超限阻斷回歸測試，模擬 AI 或用戶嘗試突破紅線的情境。

Purpose: 透過測試保證安全紅線系統的正確性，防止未來代碼異動導致安全漏洞。
Output: 全新的 Safety Redlines 回歸測試套件。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/ETF_TW/scripts/pre_flight_gate.py
@skills/ETF_TW/tests/test_fuse_v1.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立紅線系統回歸測試</name>
  <files>skills/ETF_TW/tests/test_safety_redlines.py</files>
  <action>
    撰寫 `test_safety_redlines.py` 測試套件，覆蓋以下場景：
    1. `test_max_shares_blocking`: 測試買入 2000 股 (當上限為 1000 股時) 被阻斷。
    2. `test_max_amount_twd_blocking`: 測試買入 100 萬 TWD (當上限為 50 萬時) 被阻斷。
    3. `test_max_amount_pct_blocking`: 測試買入超過 50% 現金金額被阻斷。
    4. `test_daily_loss_circuit_breaker`: 模擬 `circuit_breaker_triggered: true` 並驗證所有買入被阻斷。
    5. `test_ai_confidence_blocking`: 模擬 AI 信心 0.5 (當門檻 0.7 時) 並驗證被阻斷。
    6. `test_safety_redlines_disabled`: 驗證紅線系統 `enabled: false` 時不影響正常交易。
  </action>
  <verify>
    <automated>pytest skills/ETF_TW/tests/test_safety_redlines.py -v</automated>
  </verify>
  <done>回歸測試套件撰寫完成並通過</done>
</task>

<task type="auto">
  <name>Task 2: 將測試整合進全量測試路徑</name>
  <files>skills/ETF_TW/tests/test_safety_redlines.py</files>
  <action>
    確保測試檔案能獨立執行且不依賴外部網絡。使用 Mocking 技術處理資產、價格與紅線設定。
  </action>
  <verify>
    <automated>pytest skills/ETF_TW/tests/test_safety_redlines.py</automated>
  </verify>
  <done>測試通過並具備穩定性</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testing -> Code | 測試腳本用於驗證安全邊界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Information Disclosure | Test logs | accept | 測試日誌不包含真實 PII 或 API keys |
</threat_model>

<verification>
所有紅線阻斷測試均回報 PASSED。
</verification>

<success_criteria>
1. `test_safety_redlines.py` 包含至少 6 個核心場景。
2. 測試成功捕捉到 pre_flight_gate 的阻斷理由。
3. 測試執行時間 < 10 秒。
</success_criteria>

<output>
After completion, create `.planning/phases/09-safety-redlines/09-03-SUMMARY.md`
</output>
