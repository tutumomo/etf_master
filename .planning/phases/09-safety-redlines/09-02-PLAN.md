---
phase: 09-safety-redlines
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: [skills/ETF_TW/dashboard/app.py, skills/ETF_TW/dashboard/templates/overview.html]
autonomous: false
requirements: [FUSE-V3-REDLINE]

must_haves:
  truths:
    - "Dashboard has a '交易閾值 / Safety Redlines' section"
    - "Users can edit and save threshold parameters"
    - "All threshold fields have tooltips"
  artifacts:
    - path: "skills/ETF_TW/dashboard/templates/overview.html"
      provides: "Redline management UI"
  key_links:
    - from: "overview.html"
      to: "/api/safety-redlines/update"
      via: "fetch POST"
      pattern: "fetch.*/api/safety-redlines/update"
---

<objective>
在 Dashboard 實作 Safety Redlines 管理區塊。

Purpose: 提供視覺化介面讓用戶管理交易紅線，並為新手提供 Tooltip 說明。
Output: 包含閾值管理功能的 Dashboard。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/ETF_TW/dashboard/app.py
@skills/ETF_TW/dashboard/templates/overview.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: 實作 Dashboard 紅線管理 API</name>
  <files>skills/ETF_TW/dashboard/app.py</files>
  <action>
    在 `app.py` 中新增以下端點：
    1. `GET /api/safety-redlines`: 讀取並回傳 `safety_redlines.json`。
    2. `POST /api/safety-redlines/update`: 接收新的閾值設定並存入 `safety_redlines.json`。
    3. 確保在 `index` 路由中將紅線設定傳入模板變數。
  </action>
  <verify>
    <automated>curl -X GET http://localhost:5000/api/safety-redlines</automated>
  </verify>
  <done>API 支援讀寫紅線設定</done>
</task>

<task type="auto">
  <name>Task 2: 新增 Dashboard 閾值管理 UI</name>
  <files>skills/ETF_TW/dashboard/templates/overview.html</files>
  <action>
    在 `overview.html` 中新增一個 `card full` 區塊：
    1. 標題：交易閾值與安全紅線 (Safety Redlines)。
    2. 欄位包括：
       - 單筆買入金額上限 (TWD / Cash %)。
       - 單筆買入股數上限。
       - 個股集中度上限 (%)。
       - 日虧損熔斷比例 (%)。
       - AI 信心門檻 (0.0~1.0)。
    3. 為每個欄位添加 `<div class="tooltip">` 並附上白話說明。
    4. 實作 `saveSafetyRedlines()` JS 函式。
  </action>
  <verify>
    <automated>grep "Safety Redlines" skills/ETF_TW/dashboard/templates/overview.html</automated>
  </verify>
  <done>Dashboard UI 包含紅線管理區塊</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: 視覺檢查紅線區塊</name>
  <what-built>Dashboard 安全紅線管理區塊</what-built>
  <how-to-verify>
    1. 啟動 Dashboard (bash skills/ETF_TW/scripts/start_dashboard.sh)。
    2. 訪問 http://localhost:5000。
    3. 找到「交易閾值與安全紅線」區塊。
    4. 檢查欄位是否齊全，Tooltip 是否顯示正確。
    5. 嘗試修改一個值並點擊保存，確認頁面刷新後值已持久化。
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> Browser | 用戶透過 UI 調整紅線，UI 應限制輸入範圍 (如 0~100%) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | update API | mitigate | 在伺服器端驗證數值合法性，防止惡意注入極端值 |
</threat_model>

<verification>
Dashboard 頁面成功顯示並能儲存紅線設定。
</verification>

<success_criteria>
1. Dashboard 有獨立的紅線管理卡片。
2. 支援所有 6 個閾值參數的調整。
3. Tooltips 內容包含：買入上限、集中度、日虧損、AI 信心。
</success_criteria>

<output>
After completion, create `.planning/phases/09-safety-redlines/09-02-SUMMARY.md`
</output>
