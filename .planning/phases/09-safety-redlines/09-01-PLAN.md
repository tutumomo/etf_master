---
phase: 09-safety-redlines
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skills/ETF_TW/scripts/pre_flight_gate.py, skills/ETF_TW/scripts/sync_daily_pnl.py]
autonomous: true
requirements: [FUSE-V3-REDLINE]

must_haves:
  truths:
    - "pre_flight_gate.py can block orders exceeding max_buy_amount_twd"
    - "pre_flight_gate.py can block orders exceeding max_buy_amount_pct (cash %)"
    - "pre_flight_gate.py can block orders exceeding max_buy_shares"
    - "pre_flight_gate.py can block orders exceeding max_concentration_pct"
    - "pre_flight_gate.py can block orders when daily_loss_circuit_breaker is triggered"
    - "pre_flight_gate.py can block orders below ai_confidence_threshold"
  artifacts:
    - path: "skills/ETF_TW/scripts/sync_daily_pnl.py"
      provides: "Daily PnL tracking and circuit breaker state"
    - path: "skills/ETF_TW/scripts/pre_flight_gate.py"
      provides: "Hard redline enforcement"
  key_links:
    - from: "pre_flight_gate.py"
      to: "STATE/safety_redlines.json"
      via: "safe_load_json"
      pattern: "load_state.*safety_redlines.json"
---

<objective>
實作 Safety Redlines 的後端邏輯與交易閘門整合。

Purpose: 將交易閾值參數化，並作為「絕對紅線」硬性整合進交易保險絲，確保即使 AI 或用戶誤操作，訂單也會被阻斷。
Output: 更新後的 pre_flight_gate.py 與新增的日損益追蹤腳本。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/ETF_TW/scripts/pre_flight_gate.py
@skills/ETF_TW/scripts/sizing_engine_v1.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 初始化 Safety Redlines 數據結構</name>
  <files>skills/ETF_TW/scripts/sync_daily_pnl.py</files>
  <action>
    建立 `sync_daily_pnl.py` 並實作以下功能：
    1. 定義 `safety_redlines.json` 預設值：
       - `max_buy_amount_twd`: 500,000 (NTD)
       - `max_buy_amount_pct`: 50.0 (%) (佔現金比例)
       - `max_buy_shares`: 1000 (股)
       - `max_concentration_pct`: 30.0 (%)
       - `daily_loss_limit_pct`: 5.0 (%)
       - `ai_confidence_threshold`: 0.7
       - `enabled`: true
    2. 實作 `update_daily_baseline()`: 每日第一次執行時，記錄當前 `total_equity` 作為 `starting_equity_of_day` 存於 `daily_pnl.json`。
    3. 實作 `check_circuit_breaker()`: 計算 `(current_equity - starting_equity) / starting_equity`，若損失超過 `daily_loss_limit_pct`，則在 `daily_pnl.json` 標記 `circuit_breaker_triggered: true`。
  </action>
  <verify>
    <automated>python3 skills/ETF_TW/scripts/sync_daily_pnl.py --update-baseline && ls instances/$(python3 skills/ETF_TW/scripts/instance_identity.py)/state/daily_pnl.json</automated>
  </verify>
  <done>安全紅線設定與日損益追蹤腳本就緒</done>
</task>

<task type="auto">
  <name>Task 2: 將 Safety Redlines 整合進 pre_flight_gate.py</name>
  <files>skills/ETF_TW/scripts/pre_flight_gate.py</files>
  <action>
    修改 `check_order` 函式：
    1. 載入 `safety_redlines.json` 與 `daily_pnl.json`。
    2. 優先檢查 `circuit_breaker_triggered`: 若為 true，阻斷所有買入 (side='buy')。
    3. 整合買入金額紅線 (per D-09)：
       - 檢查金額不超過 `max_buy_amount_twd`。
       - 檢查金額不超過 `cash * (max_buy_amount_pct / 100)`。
    4. 整合股數紅線：檢查 `quantity` 不超過 `max_buy_shares`。
    5. 整合 AI 信心紅線：若 order 包含 `ai_confidence` 且低於 `ai_confidence_threshold`，則阻斷。
    6. 確保這些檢查是「絕對紅線」，失敗即回傳 `passed: False` 並帶有明確 reason。
  </action>
  <verify>
    <automated>python3 -c "from skills.ETF_TW.scripts.pre_flight_gate import check_order; print(check_order({'symbol':'0050','side':'buy','quantity':100000}, {'cash':1000000}))" | grep "passed': False"</automated>
  </verify>
  <done>pre_flight_gate 成功執行紅線檢查</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard -> API | 用戶輸入的閾值必須驗證格式與範圍 |
| API -> pre_flight_gate | 訂單參數必須由閘門二次驗證，不信任 UI 預覽結果 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | safety_redlines.json | mitigate | 使用 atomic_save_json 確保寫入完整性 |
| T-09-02 | Denial of Service | pre_flight_gate.py | mitigate | 確保 json 載入失敗時有保守的預設行為 (fail-closed) |
| T-09-03 | Information Disclosure | daily_pnl.json | mitigate | 存放在 instances 隔離目錄中 |
</threat_model>

<verification>
python3 skills/ETF_TW/scripts/pre_flight_gate.py 測試腳本通過
</verification>

<success_criteria>
1. `safety_redlines.json` 包含所有 6 個核心參數。
2. `pre_flight_gate.py` 能阻斷超大訂單 (股數/金額)。
3. `pre_flight_gate.py` 能阻斷信心不足的 AI 訂單。
4. `daily_pnl.json` 正確記錄日起始資產。
</success_criteria>

<output>
After completion, create `.planning/phases/09-safety-redlines/09-01-SUMMARY.md`
</output>
