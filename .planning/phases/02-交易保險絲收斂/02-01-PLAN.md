---
phase: 02-交易保險絲收斂
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/ETF_TW/scripts/sizing_engine_v1.py
  - skills/ETF_TW/scripts/pre_flight_gate.py
  - skills/ETF_TW/tests/test_fuse_v1.py
autonomous: true
requirements: [FUSE-01, FUSE-02, FUSE-04, FUSE-05, GIT-01, GIT-02]

must_haves:
  truths:
    - "Sizing engine correctly calculates quantity based on cash and limits"
    - "Pre-flight gate blocks invalid orders (quantity=0, price=0, over-limit)"
    - "Unit tests pass for all core fuse logic"
  artifacts:
    - path: "skills/ETF_TW/scripts/sizing_engine_v1.py"
      provides: "Sizing logic (v1)"
    - path: "skills/ETF_TW/scripts/pre_flight_gate.py"
      provides: "Unified pre-trade validation gate"
    - path: "skills/ETF_TW/tests/test_fuse_v1.py"
      provides: "Regression tests for fuse logic"
  key_links:
    - from: "skills/ETF_TW/scripts/pre_flight_gate.py"
      to: "skills/ETF_TW/scripts/sizing_engine_v1.py"
      via: "import and call"
---

<objective>
實作 sizing_engine_v1 與統一的 pre-flight gate，確保所有交易路徑（買/賣、CLI/UI）皆遵循相同的風險控制邏輯。

目的：收斂交易路徑，防止繞過風險檢查。
產出：核心風控引擎與單元測試。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@skills/ETF_TW/scripts/risk_controller.py
@skills/ETF_TW/scripts/validate_order.py
@skills/ETF_TW/scripts/trading_hours_gate.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 實作 sizing_engine_v1.py</name>
  <files>skills/ETF_TW/scripts/sizing_engine_v1.py</files>
  <behavior>
    - 輸入：可用現金 (cash)、個股集中度上限 (max_concentration_pct, e.g. 0.3)、單筆上限 (max_single_limit_twd)、風險溫度 (risk_temperature, 0~1)
    - 邏輯：
      1. 基本量 = (cash * max_concentration_pct) * risk_temperature
      2. 限制量 = min(基本量, max_single_limit_twd)
      3. 建議股數 = 限制量 // 當前價格 (取整)
      4. 判斷是否可下單 (can_order = quantity > 0)
    - 輸出：包含建議股數、限制原因 (reason)、是否可下單的 dict
  </behavior>
  <action>
    創建 sizing_engine_v1.py，實作上述計算邏輯。
    確保處理價格為 0 或負數的異常情況。
  </action>
  <verify>
    <automated>python3 -c "from skills.ETF_TW.scripts.sizing_engine_v1 import calculate_size; print(calculate_size(1000000, 0.3, 200000, 1.0, 100))" | grep "'quantity': 2000"</automated>
  </verify>
  <done>Sizing 引擎實作完成且基本邏輯正確</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 實作統一的 pre_flight_gate.py</name>
  <files>skills/ETF_TW/scripts/pre_flight_gate.py</files>
  <behavior>
    - 檢查項 (FUSE-02)：
      1. 數量必須大於 0
      2. 限價單價格必須大於 0
      3. 集中度與單筆上限 (呼叫 sizing_engine_v1)
      4. 交易時段檢查 (呼叫 trading_hours_gate.py)
      5. 張/股單位檢查 (1000 股為一張，零股必須為 1~999)
      6. 賣出庫存檢查 (需傳入目前庫存數量)
    - 買賣雙向走同一路徑，不因 side 不同而略過檢查。
  </behavior>
  <action>
    創建 pre_flight_gate.py，整合現有的 risk_controller 與 trading_hours 邏輯。
    定義一個統一的 check_order(order, context) 函式。
  </action>
  <verify>
    <automated>python3 -c "from skills.ETF_TW.scripts.pre_flight_gate import check_order; print(check_order({'symbol': '0050', 'side': 'buy', 'quantity': 0, 'price': 150}, {}))" | grep "'passed': False"</automated>
  </verify>
  <done>統一 Pre-flight Gate 實作完成，涵蓋所有指定檢查項</done>
</task>

<task type="auto">
  <name>Task 3: 建立風控單元測試</name>
  <files>skills/ETF_TW/tests/test_fuse_v1.py</files>
  <action>
    實作單元測試：
    - 測試超限被擋 (Over concentration/limit)
    - 測試超庫存被擋 (Short sell check)
    - 測試非法價格/數量
    - 測試非交易時段擋單
    - 測試張/股單位異常
  </action>
  <verify>
    <automated>pytest skills/ETF_TW/tests/test_fuse_v1.py</automated>
  </verify>
  <done>單元測試通過，證明風控可準確擋單 (FUSE-04)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CLI/UI -> Fuse | 外部輸入進入風控引擎，必須校驗資料型態 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Pre-flight Gate | mitigate | 確保所有下單入口（submit_order）強制呼叫 pre-flight gate |
| T-02-02 | Denial of Service | Sizing Engine | mitigate | 處理價格為 0 或極端值的情況，避免除以零 |
</threat_model>

<verification>
python3 skills/ETF_TW/tests/test_fuse_v1.py
</verification>

<success_criteria>
1. sizing_engine_v1 計算結果符合預期。
2. pre_flight_gate 可成功擋下無效訂單。
3. 單元測試 100% 通過。
</success_criteria>

<output>
After completion, create .planning/phases/02-交易保險絲收斂/02-01-SUMMARY.md
</output>
