---
phase: "02"
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - skills/ETF_TW/assets/config.json
  - skills/ETF_TW/scripts/refresh_monitoring_state.py
  - skills/ETF_TW/scripts/dashboard/templates/overview.html
autonomous: true
requirements: [FUSE-06, GIT-01, GIT-02]

must_haves:
  truths:
    - "Trading thresholds can be configured in assets/config.json"
    - "Dashboard displays trigger suggestions when thresholds are met"
  artifacts:
    - path: "skills/ETF_TW/assets/config.json"
      provides: "Trading thresholds configuration"
    - path: "skills/ETF_TW/scripts/refresh_monitoring_state.py"
      provides: "Threshold monitoring and suggestion triggering"
  key_links:
    - from: "skills/ETF_TW/scripts/refresh_monitoring_state.py"
      to: "skills/ETF_TW/assets/config.json"
      via: "loading trading_thresholds"
    - from: "skills/ETF_TW/scripts/dashboard/templates/overview.html"
      to: "skills/ETF_TW/state/snapshot.json"
      via: "displaying suggestions from state"
---

<objective>
實作交易閾值設定與 Dashboard 觸發建議。

目的：提供視覺化的交易建議與簡化交易流程。
產出：可配置的閾值系統與具備「下單建議」按鈕的 Dashboard。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@skills/ETF_TW/assets/config.json
@skills/ETF_TW/scripts/refresh_monitoring_state.py
@skills/ETF_TW/scripts/dashboard/templates/overview.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 assets/config.json 加入交易閾值設定</name>
  <files>skills/ETF_TW/assets/config.json</files>
  <action>
    在 `assets/config.json` 中加入 `trading_thresholds` 物件：
    - `cash_percentage_min`: 現金佔比下限 (例如 10%)。
    - `symbol_weight_max`: 單一標的佔比上限 (例如 30%)。
    - 其他必要閾值。
  </action>
  <verify>
    <automated>grep "trading_thresholds" skills/ETF_TW/assets/config.json</automated>
  </verify>
  <done>交易閾值設定已加入設定檔</done>
</task>

<task type="auto">
  <name>Task 2: 修改 refresh_monitoring_state.py 檢查閾值並產生建議</name>
  <files>skills/ETF_TW/scripts/refresh_monitoring_state.py</files>
  <action>
    1. 讀取 `trading_thresholds` 設定。
    2. 在更新快照時，檢查目前的持倉與現金佔比是否違反閾值。
    3. 如果觸發閾值，在 state (snapshot.json) 中加入 `trigger_suggestions` 欄位。
    4. 建議內容包含標的、方向、原因與建議動作。
  </action>
  <verify>
    <automated>grep "trigger_suggestions" skills/ETF_TW/scripts/refresh_monitoring_state.py</automated>
  </verify>
  <done>閾值監控與建議機制實作完成</done>
</task>

<task type="auto">
  <name>Task 3: 修改 overview.html 顯示下單建議按鈕</name>
  <files>skills/ETF_TW/scripts/dashboard/templates/overview.html</files>
  <action>
    1. 檢查 state 中是否存在 `trigger_suggestions`。
    2. 如果存在，在持倉區塊（或對應區塊）顯示顯眼的「下單建議」或「交易」按鈕。
    3. 按鈕應能導向交易頁面或展開交易票據。
  </action>
  <verify>
    <automated>grep "trigger_suggestions" skills/ETF_TW/scripts/dashboard/templates/overview.html</automated>
  </verify>
  <done>Dashboard 已具備下單建議顯示功能</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User -> UI | 點擊建議按鈕後的送單安全性 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | config.json | mitigate | 設定檔權限控制，確保只有授權使用者可修改閾值 |
| T-02-07 | Spoofing | dashboard triggers | mitigate | 建議按鈕觸發的交易仍須通過 pre-flight gate 與人工確認 |
</threat_model>

<verification>
手動調整閾值以觸發建議，並檢查 Dashboard 是否正確顯示按鈕。
</verification>

<success_criteria>
1. `assets/config.json` 包含 `trading_thresholds`。
2. `refresh_monitoring_state.py` 能正確識別閾值觸發。
3. Dashboard 顯示顯眼的交易建議按鈕。
</success_criteria>

<output>
After completion, create .planning/phases/02-fuse-convergence/02-03-SUMMARY.md
</output>
