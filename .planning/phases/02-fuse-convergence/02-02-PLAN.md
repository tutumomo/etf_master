---
phase: "02"
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - skills/ETF_TW/scripts/adapters/base.py
  - skills/ETF_TW/scripts/adapters/paper_adapter.py
  - skills/ETF_TW/scripts/adapters/sinopac_adapter.py
  - skills/ETF_TW/scripts/submit_verification.py
autonomous: true
requirements: [FUSE-01, FUSE-03, GIT-01, GIT-02]

must_haves:
  truths:
    - "Adapters enforce pre-flight gate before calling broker API"
    - "Post-submit verification correctly detects landed vs non-landed orders"
    - "Submit result includes truth level based on verification"
  artifacts:
    - path: "skills/ETF_TW/scripts/submit_verification.py"
      provides: "Post-submit verification logic"
  key_links:
    - from: "skills/ETF_TW/scripts/adapters/base.py"
      to: "skills/ETF_TW/scripts/pre_flight_gate.py"
      via: "check_order call"
    - from: "skills/ETF_TW/scripts/adapters/base.py"
      to: "skills/ETF_TW/scripts/submit_verification.py"
      via: "verify_landing call"
---

<objective>
將風控門禁與送單後驗證整合進 Adapter 流程，確保「送單成功」等同於「已看到委託落地」。

目的：落實真相層級治理，消除送單後的黑盒期。
產出：具備自動驗證能力的交易 Adapter。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-fuse-convergence/02-01-SUMMARY.md
@skills/ETF_TW/scripts/adapters/base.py
@skills/ETF_TW/scripts/adapters/paper_adapter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 實作 submit_verification.py</name>
  <files>skills/ETF_TW/scripts/submit_verification.py</files>
  <action>
    實作 verify_order_landing(adapter, order_id, timeout=10) 函式：
    1. 呼叫 adapter.list_trades() 或 adapter.get_order_status()。
    2. 如果能在清單中找到對應的 order_id 且狀態不為 rejected，則判定為「已落地」。
    3. 如果超時未見，則判定為「未落地/待確認」。
    4. 回傳包含驗證結果與建議動作的 dict。
  </action>
  <verify>
    <automated>python3 -c "from skills.ETF_TW.scripts.submit_verification import verify_order_landing; print('verify_order_landing' in dir())"</automated>
  </verify>
  <done>落地驗證模組開發完成</done>
</task>

<task type="auto">
  <name>Task 2: 在 BaseAdapter 整合門禁與驗證</name>
  <files>skills/ETF_TW/scripts/adapters/base.py</files>
  <action>
    1. 在 BaseAdapter.submit_order 增加通用邏輯（或提供 _pre_submit_check 工具函式）。
    2. 強制所有繼承者在真正呼叫券商 API 前執行 pre_flight_gate.check_order。
    3. 在券商 API 回傳成功後，自動呼叫 submit_verification 進行落地確認。
  </action>
  <verify>
    <automated>grep "pre_flight_gate" skills/ETF_TW/scripts/adapters/base.py</automated>
  </verify>
  <done>BaseAdapter 具備保險絲控制力</done>
</task>

<task type="auto">
  <name>Task 3: 更新具體 Adapter 實作</name>
  <files>skills/ETF_TW/scripts/adapters/paper_adapter.py, skills/ETF_TW/scripts/adapters/sinopac_adapter.py</files>
  <action>
    1. 更新 PaperAdapter 與 SinopacAdapter 的 submit_order。
    2. 確保它們符合 BaseAdapter 的新契約，不會繞過門禁。
    3. 修正 PaperAdapter 以模擬落地延遲，測試驗證機制。
  </action>
  <verify>
    <automated>pytest skills/ETF_TW/tests/test_fuse_v1.py</automated>
  </verify>
  <done>現有 Adapter 已全部收斂至新風控路徑</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Adapter -> Broker API | 呼叫外部 API 前後的真相落差 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | list_trades | mitigate | 處理 list_trades 回傳空值的情況，不應誤報為失敗 |
| T-02-04 | Repudiation | submit_verification | mitigate | 記錄驗證日誌，證明「何時、如何」確認委託落地 |
</threat_model>

<verification>
執行模擬交易並觀察輸出是否包含 [驗證中] 或 [已落地] 標籤。
</verification>

<success_criteria>
1. 所有 Adapter 送單前皆有 pre-flight gate 檢查。
2. 送單後自動觸發落地驗證。
3. 測試環境中，未落地的訂單不會被標記為成功。
</success_criteria>

<output>
After completion, create .planning/phases/02-fuse-convergence/02-02-SUMMARY.md
</output>
