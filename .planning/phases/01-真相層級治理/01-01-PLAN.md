---
phase: 01-truth-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "skills/ETF_TW/SKILL.md",
  "skills/ETF_TW/README.md",
  "skills/ETF_TW/docs/STATE_ARCHITECTURE.md",
  "skills/ETF_TW/scripts/adapters/sinopac_adapter.py",
  "skills/ETF_TW/scripts/etf_tw.py"
]
autonomous: true
requirements: [TRUTH-02, TRUTH-03, TRUTH-04, GIT-01, GIT-02]

must_haves:
  truths:
    - "活文件中不再出現『state 是唯一真相源』或類似說法"
    - "list_trades 為空時的回應採用保守措辭（如：本次查詢未見委託）"
    - "submit 回應明確包含『仍需後續驗證』字樣"
  artifacts:
    - path: "skills/ETF_TW/docs/STATE_ARCHITECTURE.md"
      provides: "定義三層分級真相結構"
    - path: "skills/ETF_TW/scripts/adapters/sinopac_adapter.py"
      provides: "保守措辭處理 list_trades"
  key_links:
    - from: "scripts/etf_tw.py"
      to: "Sinopac API"
      via: "submit order response labeling"
---

<objective>
建立真相層級治理的基礎：統一文件規範與修正核心委託查詢/送單的保守措辭。

目的：消除對本機 state 的過度信任，建立「驗證優於回傳」的風險意識。
輸出：更新的文件、具備保守措辭的 Adapter 與 CLI 顯示。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/ETF_TW/SKILL.md
@skills/ETF_TW/docs/STATE_ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新文件並移除「唯一真相源」說法</name>
  <files>skills/ETF_TW/SKILL.md, skills/ETF_TW/README.md, skills/ETF_TW/docs/STATE_ARCHITECTURE.md, skills/ETF_TW/INSTALL.md, skills/ETF_TW/CHANGELOG.md</files>
  <action>
    - 在 `SKILL.md` 與 `STATE_ARCHITECTURE.md` 中定義三層真相分級：
      1. Level 1: Live Direct Evidence (券商 API 即時查得之成交/掛單/庫存)
      2. Level 2: Live Unconfirmed (送單回傳成功但尚未查得落地證據)
      3. Level 3: Secondary Info (本機 state 檔案、dashboard 衍生顯示、歷史紀錄)
    - 搜尋並修改所有將 `state` 稱為「唯一真相源 (SSoT)」的句子，改為「本機狀態快照」或「次級資訊來源」。
    - 在 `INSTALL.md` 中同步此觀念。
    - 在 `CHANGELOG.md` 紀錄此治理變更。
    - **Commit**: `docs(truth): define 3-layer truth hierarchy and remove SSoT claims` (per GIT-01, GIT-02)
  </action>
  <verify>
    <automated>grep -r "唯一真相源" skills/ETF_TW/ | wc -l</automated>
  </verify>
  <done>所有活文件中不再有「state 是唯一真相源」的說法，且定義了三層分級。</done>
</task>

<task type="auto">
  <name>Task 2: 修正 list_trades 與 submit 的回應措辭</name>
  <files>skills/ETF_TW/scripts/adapters/sinopac_adapter.py, skills/ETF_TW/scripts/etf_tw.py, skills/ETF_TW/scripts/submit_verification.py</files>
  <action>
    - 修改 `sinopac_adapter.py`：在 `list_trades` 邏輯中，若 `self.api.list_trades()` 回傳空列表，回應文字應明確包含「本次查詢沒看到任何委託，不代表委託失敗或已成交，請稍後再試或檢查網路」。
    - 修改 `etf_tw.py` 與 `submit_verification.py`：在 `submit` 委託執行後，回傳給使用者的文字必須標註「[驗證中] 委託已送出，仍需後續驗證（list_trades）確認落地事實」。
    - **Commit**: `feat(truth): implement conservative wording for list_trades and submit` (per GIT-01, GIT-02)
  </action>
  <verify>
    <automated>grep -E "本次查詢沒看到|仍需後續驗證" skills/ETF_TW/scripts/</automated>
  </verify>
  <done>委託查詢空值時不再暗示失敗，送單回應明確要求驗證。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Code → User | 資訊傳達邊界，誤導性詞彙可能導致錯誤交易決策 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure (False) | CLI Output | mitigate | 強制採用保守措辭，不反推未知狀態 |
| T-01-02 | Repudiation | submit response | mitigate | 標示「仍需後續驗證」，防止使用者誤認送單即成功 |
</threat_model>

<verification>
1. 執行 `grep -r "唯一真相源" skills/ETF_TW/` 確認無殘留。
2. 閱讀 `docs/STATE_ARCHITECTURE.md` 確認三層分級定義明確。
3. 檢查 `scripts/adapters/sinopac_adapter.py` 確認 list_trades 措辭變更。
</verification>

<success_criteria>
1. 文件中「真相源」說法已與程式邏輯對齊。
2. 程式輸出的不確定性已通過措辭明確傳達。
</success_criteria>

<output>
After completion, create `.planning/phases/01-truth-governance/01-01-SUMMARY.md`
</output>
