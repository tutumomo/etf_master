---
phase: 01-truth-governance
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [
  "skills/ETF_TW/scripts/ai_decision_bridge.py",
  "skills/ETF_TW/scripts/run_auto_decision_scan.py",
  "skills/ETF_TW/dashboard/app.py",
  "skills/ETF_TW/dashboard/templates/overview.html"
]
autonomous: true
requirements: [TRUTH-05, GIT-01, GIT-02]

must_haves:
  truths:
    - "AI 建議生成後能正確寫入 ai_decision_response.json"
    - "Consensus Arbitration 能正確處理 AI 與規則引擎的衝突並產出仲裁結果"
    - "Dashboard 能同時顯示 AI 建議與規則引擎建議，並明確標註來源"
  artifacts:
    - path: "skills/ETF_TW/scripts/ai_decision_bridge.py"
      provides: "AI 決策橋接邏輯"
    - path: "skills/ETF_TW/scripts/run_auto_decision_scan.py"
      provides: "Consensus (共識仲裁) 實作"
  key_links:
    - from: "dashboard/app.py"
      to: "ai_decision_response.json"
      via: "JSON load and display"
---

<objective>
確保決策控制台的「AI 建議」與「規則引擎建議」雙軌機制完全落地。

目的：提供多重決策佐證，降低單一系統誤判風險。
輸出：經驗證的 AI Bridge 鏈路、具備共識仲裁能力的掃描腳本、更新後的 Dashboard 介面。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/ETF_TW/docs/AI_DECISION_BRIDGE.md
@skills/ETF_TW/scripts/ai_decision_bridge.py
@skills/ETF_TW/scripts/run_auto_decision_scan.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 驗證 AI Decision Bridge 與 Consensus 鏈路</name>
  <files>skills/ETF_TW/scripts/ai_decision_bridge.py, skills/ETF_TW/scripts/run_auto_decision_scan.py</files>
  <action>
    - 審核 `ai_decision_bridge.py`：確保 `request_id` 與 `expires_at` 處理正確，且支援 `consensus` 狀態寫回。
    - 審核 `run_auto_decision_scan.py`：確認 `resolve_consensus()` 函數邏輯。
    - 確保 `resolve_consensus()` 能根據 `ai_decision_response.json` (若存在且未過期) 與規則引擎結果產出 `consensus` 區塊，並寫入 `auto_preview_candidate.json`。
    - 修正任何鏈路中斷點（如路徑錯誤、欄位缺失）。
    - **Commit**: `fix(truth): ensure AI bridge and consensus arbitration connectivity` (per GIT-01, GIT-02)
  </action>
  <verify>
    <automated>python3 skills/ETF_TW/scripts/run_auto_decision_scan.py --test-consensus</automated>
  </verify>
  <done>AI 建議與規則引擎的共識機制已接通且可產出仲裁結果。</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard 雙軌建議顯示落地</name>
  <files>skills/ETF_TW/dashboard/app.py, skills/ETF_TW/dashboard/templates/overview.html</files>
  <action>
    - 修改 `dashboard/app.py`：在 `/api/overview` 或相關端點中，同時回傳 AI 建議 (`ai_decision_response`) 與 規則引擎建議 (`preview_candidate`) 的內容與來源。
    - 修改 `overview.html`：
      - 在交易建議區塊，明確區分「AI 建議 (來自 Bridge)」與「系統建議 (來自 Rule Engine)」。
      - 標註建議的 freshness (stale/fresh)。
      - 顯示仲裁結果 (Consensus) 及其 Tier 層級。
      - 修改頁面上的真相源提示，移除「state 是唯一真相源」的文字，改為「本機 state 快照」。
    - **Commit**: `feat(truth): implement dual-lane suggestion display in dashboard` (per GIT-01, GIT-02)
  </action>
  <verify>
    <automated>curl -s http://localhost:5055/api/overview | grep -E "ai_decision|preview_candidate"</automated>
  </verify>
  <done>Dashboard 介面已能正確顯示雙軌建議與仲裁資訊，且用語已更正。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI Agent → System | 外部建議輸入邊界，需通過仲裁層過濾 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | AI Response | mitigate | 通過 `resolve_consensus` 進行三層仲裁，規則引擎具備否決權 |
</threat_model>

<verification>
1. 觀察 `auto_preview_candidate.json` 是否包含正確的 `consensus` 欄位。
2. 開啟 Dashboard 確認建議區塊顯示 AI 與 Rule Engine 來源。
</verification>

<success_criteria>
1. 決策控制台全鏈路通暢。
2. 雙軌建議機制在 Dashboard 可見且來源明確。
</success_criteria>

<output>
After completion, create `.planning/phases/01-truth-governance/01-03-SUMMARY.md`
</output>
